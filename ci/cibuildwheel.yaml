trigger: none

stages:
  - stage: cibuildwheel
    condition: eq(variables['System.PullRequest.IsFork'], 'false')
    jobs:
      - job: linux_arm64_manylinux_a
        pool: hetzner-incus-arm
        timeoutInMinutes: 120
        condition: eq(variables['System.PullRequest.IsFork'], 'false')
        steps:
          - checkout: self
            fetchDepth: 1
            lfs: false
            submodules: true
          - bash: |
              set -o errexit
              sudo find /etc/apt -name '*.list' -o -name '*.sources' \
                  | sudo xargs -r sed -i 's|http://ports.ubuntu.com|https://ports.ubuntu.com|g'
              sudo apt-get update --error-on=any
              sudo apt-get install -y python3-pip python3-venv
              python3 -m pip install --upgrade pip --break-system-packages
              python3 -m pip install cibuildwheel --break-system-packages
            displayName: Install dependencies
          - bash: python3 -m cibuildwheel --output-dir wheelhouse .
            displayName: Build wheels
            env:
              CIBW_BUILD: "cp310-manylinux_aarch64 cp311-manylinux_aarch64 cp312-manylinux_aarch64"
          - task: PublishBuildArtifacts@1
            inputs: {pathtoPublish: 'wheelhouse'}

      - job: linux_arm64_manylinux_b
        pool: hetzner-incus-arm
        timeoutInMinutes: 120
        condition: eq(variables['System.PullRequest.IsFork'], 'false')
        steps:
          - checkout: self
            fetchDepth: 1
            lfs: false
            submodules: true
          - bash: |
              set -o errexit
              sudo find /etc/apt -name '*.list' -o -name '*.sources' \
                  | sudo xargs -r sed -i 's|http://ports.ubuntu.com|https://ports.ubuntu.com|g'
              sudo apt-get update --error-on=any
              sudo apt-get install -y python3-pip python3-venv
              python3 -m pip install --upgrade pip --break-system-packages
              python3 -m pip install cibuildwheel --break-system-packages
            displayName: Install dependencies
          - bash: python3 -m cibuildwheel --output-dir wheelhouse .
            displayName: Build wheels
            env:
              CIBW_BUILD: "cp313*-manylinux_aarch64 cp314*-manylinux_aarch64"
          - task: PublishBuildArtifacts@1
            inputs: {pathtoPublish: 'wheelhouse'}

      - job: linux_arm64_musllinux_a
        pool: hetzner-incus-arm
        timeoutInMinutes: 120
        condition: eq(variables['System.PullRequest.IsFork'], 'false')
        steps:
          - checkout: self
            fetchDepth: 1
            lfs: false
            submodules: true
          - bash: |
              set -o errexit
              sudo find /etc/apt -name '*.list' -o -name '*.sources' \
                  | sudo xargs -r sed -i 's|http://ports.ubuntu.com|https://ports.ubuntu.com|g'
              sudo apt-get update --error-on=any
              sudo apt-get install -y python3-pip python3-venv
              python3 -m pip install --upgrade pip --break-system-packages
              python3 -m pip install cibuildwheel --break-system-packages
            displayName: Install dependencies
          - bash: python3 -m cibuildwheel --output-dir wheelhouse .
            displayName: Build wheels
            env:
              CIBW_BUILD: "cp310-musllinux_aarch64 cp311-musllinux_aarch64 cp312-musllinux_aarch64"
          - task: PublishBuildArtifacts@1
            inputs: {pathtoPublish: 'wheelhouse'}

      - job: linux_arm64_musllinux_b
        pool: hetzner-incus-arm
        timeoutInMinutes: 120
        condition: eq(variables['System.PullRequest.IsFork'], 'false')
        steps:
          - checkout: self
            fetchDepth: 1
            lfs: false
            submodules: true
          - bash: |
              set -o errexit
              sudo find /etc/apt -name '*.list' -o -name '*.sources' \
                  | sudo xargs -r sed -i 's|http://ports.ubuntu.com|https://ports.ubuntu.com|g'
              sudo apt-get update --error-on=any
              sudo apt-get install -y python3-pip python3-venv
              python3 -m pip install --upgrade pip --break-system-packages
              python3 -m pip install cibuildwheel --break-system-packages
            displayName: Install dependencies
          - bash: python3 -m cibuildwheel --output-dir wheelhouse .
            displayName: Build wheels
            env:
              CIBW_BUILD: "cp313*-musllinux_aarch64 cp314*-musllinux_aarch64"
          - task: PublishBuildArtifacts@1
            inputs: {pathtoPublish: 'wheelhouse'}

      - job: linux_x64_cpython_manylinux_x86_64_a
        pool: {vmImage: 'ubuntu-latest'}
        timeoutInMinutes: 120
        steps:
          - task: UsePythonVersion@0
          - bash: |
              set -o errexit
              python3 -m pip install --upgrade pip
              python3 -m pip install cibuildwheel
            displayName: Install dependencies
          - bash: cibuildwheel --output-dir wheelhouse .
            displayName: Build wheels
            env:
              CIBW_BUILD: "cp310-manylinux_x86_64 cp311-manylinux_x86_64 cp312-manylinux_x86_64"
          - task: PublishBuildArtifacts@1
            inputs: {pathtoPublish: 'wheelhouse'}

      - job: linux_x64_cpython_manylinux_x86_64_b
        pool: {vmImage: 'ubuntu-latest'}
        timeoutInMinutes: 120
        steps:
          - task: UsePythonVersion@0
          - bash: |
              set -o errexit
              python3 -m pip install --upgrade pip
              python3 -m pip install cibuildwheel
            displayName: Install dependencies
          - bash: cibuildwheel --output-dir wheelhouse .
            displayName: Build wheels
            env:
              CIBW_BUILD: "cp313*-manylinux_x86_64 cp314*-manylinux_x86_64"
          - task: PublishBuildArtifacts@1
            inputs: {pathtoPublish: 'wheelhouse'}

      - job: linux_x64_cpython_musllinux_a
        pool: {vmImage: 'ubuntu-latest'}
        timeoutInMinutes: 120
        steps:
          - task: UsePythonVersion@0
          - bash: |
              set -o errexit
              python3 -m pip install --upgrade pip
              python3 -m pip install cibuildwheel
            displayName: Install dependencies
          - bash: cibuildwheel --output-dir wheelhouse .
            displayName: Build wheels
            env:
              CIBW_BUILD: "cp310-musllinux* cp311-musllinux* cp312-musllinux*"
          - task: PublishBuildArtifacts@1
            inputs: {pathtoPublish: 'wheelhouse'}

      - job: linux_x64_cpython_musllinux_b
        pool: {vmImage: 'ubuntu-latest'}
        timeoutInMinutes: 120
        steps:
          - task: UsePythonVersion@0
          - bash: |
              set -o errexit
              python3 -m pip install --upgrade pip
              python3 -m pip install cibuildwheel
            displayName: Install dependencies
          - bash: cibuildwheel --output-dir wheelhouse .
            displayName: Build wheels
            env:
              CIBW_BUILD: "cp313*-musllinux* cp314*-musllinux*"
          - task: PublishBuildArtifacts@1
            inputs: {pathtoPublish: 'wheelhouse'}

      - job: linux_x64_pypy
        pool: {vmImage: 'ubuntu-latest'}
        timeoutInMinutes: 120
        steps:
          - task: UsePythonVersion@0
          - bash: |
              set -o errexit
              python3 -m pip install --upgrade pip
              python3 -m pip install cibuildwheel
            displayName: Install dependencies
          - bash: cibuildwheel --output-dir wheelhouse .
            displayName: Build wheels
            env:
              CIBW_BUILD: pp*
              CIBW_ENABLE: pypy pypy-eol
          - task: PublishBuildArtifacts@1
            inputs: {pathtoPublish: 'wheelhouse'}

      - job: macos_cp310_cp311
        pool: {vmImage: 'macOS-15'}
        timeoutInMinutes: 120
        steps:
          - task: UsePythonVersion@0
          - bash: |
              set -o errexit
              python3 -m pip install --upgrade pip
              python3 -m pip install cibuildwheel
            displayName: Install dependencies
          - bash: cibuildwheel --output-dir wheelhouse .
            displayName: Build wheels
            env:
              CIBW_BUILD: "cp310-* cp311-*"
          - task: PublishBuildArtifacts@1
            inputs: {pathtoPublish: wheelhouse}

      - job: macos_cp312_cp313
        pool: {vmImage: 'macOS-15'}
        timeoutInMinutes: 120
        steps:
          - task: UsePythonVersion@0
          - bash: |
              set -o errexit
              python3 -m pip install --upgrade pip
              python3 -m pip install cibuildwheel
            displayName: Install dependencies
          - bash: cibuildwheel --output-dir wheelhouse .
            displayName: Build wheels
            env:
              CIBW_BUILD: "cp312-* cp313-*"
          - task: PublishBuildArtifacts@1
            inputs: {pathtoPublish: wheelhouse}

      - job: macos_cp314
        pool: {vmImage: 'macOS-15'}
        timeoutInMinutes: 120
        steps:
          - task: UsePythonVersion@0
          - bash: |
              set -o errexit
              python3 -m pip install --upgrade pip
              python3 -m pip install cibuildwheel
            displayName: Install dependencies
          - bash: cibuildwheel --output-dir wheelhouse .
            displayName: Build wheels
            env:
              CIBW_BUILD: "cp314-*"
          - task: PublishBuildArtifacts@1
            inputs: {pathtoPublish: wheelhouse}

      - job: macos_cp314t
        pool: {vmImage: 'macOS-15'}
        timeoutInMinutes: 120
        steps:
          - task: UsePythonVersion@0
          - bash: |
              set -o errexit
              python3 -m pip install --upgrade pip
              python3 -m pip install cibuildwheel
            displayName: Install dependencies
          - bash: cibuildwheel --output-dir wheelhouse .
            displayName: Build wheels
            env:
              CIBW_BUILD: "cp314t-*"
          - task: PublishBuildArtifacts@1
            inputs: {pathtoPublish: wheelhouse}

      - job: windows_i686
        pool: {vmImage: 'windows-2025'}
        timeoutInMinutes: 120
        steps:
          - task: UsePythonVersion@0
          - bash: |
              set -o errexit
              python3 -m pip install --upgrade pip
              python3 -m pip install cibuildwheel
            displayName: Install dependencies
          - powershell: |
              $vsPath = Resolve-Path "C:\Program Files\Microsoft Visual Studio\2022\*\VC\Auxiliary\Build\vcvars32.bat"
              cmd /c "call `"$vsPath`" && set > env_vars.txt"

              Get-Content env_vars.txt | ForEach-Object {
                if ($_ -match "^([^=]+?)=(.*)$" -and $matches[1] -notmatch '^(SYSTEM|AGENT|BUILD|RELEASE|VSTS|TASK|USE_|FAIL_|MSDEPLOY|AZP_|AZURE_HTTP|COPYFILESOVERSSHV0|ENABLE_ISSUE_SOURCE_VALIDATION|MODIFY_NUMBER_OF_RETRIES_IN_ROBOCOPY|MSBUILDHELPERS_ENABLE_TELEMETRY|RETIRE_AZURERM_POWERSHELL_MODULE|ROSETTA2_WARNING)') {
                  [System.Environment]::SetEnvironmentVariable($matches[1], $matches[2], "Process")
                  Write-Host "##vso[task.setvariable variable=$($matches[1])]$($matches[2])"
                }
              }

              where.exe cl.exe
              cibuildwheel --output-dir wheelhouse .
            displayName: Build wheels
            env:
              CIBW_ENVIRONMENT: "MSSdk=1 DISTUTILS_USE_SDK=1 SETUP_DO_GIT_SUBMODULE_INIT=1"
              CIBW_BUILD: "*win32*"
          - task: PublishBuildArtifacts@1
            inputs: {pathtoPublish: 'wheelhouse'}

      - job: windows_x86_64
        pool: {vmImage: 'windows-2025'}
        timeoutInMinutes: 120
        steps:
          - task: UsePythonVersion@0
          - bash: |
              set -o errexit
              python3 -m pip install --upgrade pip
              python3 -m pip install cibuildwheel
            displayName: Install dependencies
          - powershell: |
              $vsPath = Resolve-Path "C:\Program Files\Microsoft Visual Studio\2022\*\VC\Auxiliary\Build\vcvars64.bat"
              cmd /c "call `"$vsPath`" && set > env_vars.txt"

              Get-Content env_vars.txt | ForEach-Object {
                if ($_ -match "^([^=]+?)=(.*)$" -and $matches[1] -notmatch '^(SYSTEM|AGENT|BUILD|RELEASE|VSTS|TASK|USE_|FAIL_|MSDEPLOY|AZP_|AZURE_HTTP|COPYFILESOVERSSHV0|ENABLE_ISSUE_SOURCE_VALIDATION|MODIFY_NUMBER_OF_RETRIES_IN_ROBOCOPY|MSBUILDHELPERS_ENABLE_TELEMETRY|RETIRE_AZURERM_POWERSHELL_MODULE|ROSETTA2_WARNING)') {
                  [System.Environment]::SetEnvironmentVariable($matches[1], $matches[2], "Process")
                  Write-Host "##vso[task.setvariable variable=$($matches[1])]$($matches[2])"
                }
              }

              where.exe cl.exe
              cibuildwheel --output-dir wheelhouse .
            displayName: Build wheels
            env:
              CIBW_BUILD: "*win_amd64*"
              CIBW_ENVIRONMENT: "MSSdk=1 DISTUTILS_USE_SDK=1 SETUP_DO_GIT_SUBMODULE_INIT=1"
              CIBW_BUILD_VERBOSITY: "3"
              DISTUTILS_DEBUG: "1"
          - task: PublishBuildArtifacts@1
            inputs: {pathtoPublish: 'wheelhouse'}
